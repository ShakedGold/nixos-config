(defsrc
  esc
  caps
  lsft
  lctl
  lmet
  lalt
)

(defvar
  tap-time 200
  hold-time 200
)

(defalias
  hyperkey (tap-hold-press $tap-time $hold-time esc (multi lsft lctl lalt lmet))
)

(deflayermap (default)
  caps @hyperkey
)
